// import { useParams } from "react-router-dom";

export function About() {
  // let { name } = useParams();
  return <h1 className="about__title">Hello About Page</h1>;
  // return <h1 className="about__title">Hello {name}</h1>;
}
